import styled from "styled-components"

export default function Mbaixo() {
    return(
        <Container>
        <p>You don't have any habits registered yet. 
            Add a habit to start tracking!</p>
        </Container>
    )
}

const Container = styled.div`
margin-left: 19px;
width: 338px;
height: auto;
`